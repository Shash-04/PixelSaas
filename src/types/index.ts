export interface Video {
    id: string
    title: string
    description: string
    publicId: string
    originalSize: String
    compressedSize: String
    duration: number
    createdAt: Date
    updatedAt: Date
}
